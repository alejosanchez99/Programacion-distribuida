const pet = {
    name: 'Bendict',
    type: 'Dog',
    agePet: 1
}

console.log(pet);


const { name, type, agePet } = pet

console.log(name);
console.log(type);
console.log(agePet);

// Con la destrucción del objeto podemos obtener cada elemento en una variable independiente