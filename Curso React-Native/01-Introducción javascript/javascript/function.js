
const sum = (a,b) => {
    return a + b;
}

console.log(sum(1,3));


const a = 12;
const b = 14;
const result = sum(a,b);

console.log(`El resultado de la suma de ${a}+${b} es: ${result}`);

// En la primera parte vemos la construcción de una función flecha
// y en la otra expresión vemos el template string que permite concatenar varios 
// resultados dentro de un texto.